[project]
name = "ecoloop-common"
version = "0.1.0"
description = "Framework-independent shared primitives for EcoLoop AI"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.32,<0.12"]
build-backend = "uv_build"
